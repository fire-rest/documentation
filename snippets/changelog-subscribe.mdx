{/*
  Inline subscribe form for the changelog, shared by EN / ES / PT.

  Posts straight to Buttondown, which owns the whole list: double opt-in,
  confirmation email, unsubscribe and archive. No address ever reaches this repo.

  `fire-docs` in LIST_URL is a PLACEHOLDER — replace it with the real Buttondown
  username once the account exists. This file and docs.json are the only two
  places it appears.

  All copy comes in as props so the three languages stay in their own changelog
  files; this snippet holds only markup.
*/}

export const LIST_URL = 'https://buttondown.com/api/emails/embed-subscribe/fire-docs'

export default ({ title, body, placeholder, button }) => (
  <div
    style={{
      border: '1px solid rgb(128 128 128 / 0.25)',
      borderRadius: '0.75rem',
      padding: '1.25rem',
      marginBottom: '2rem',
    }}
  >
    <strong>{title}</strong>
    <p style={{ marginTop: '0.375rem', fontSize: '0.875rem', opacity: 0.85 }}>{body}</p>
    <form
      action={LIST_URL}
      method="post"
      target="_blank"
      style={{ display: 'flex', flexWrap: 'wrap', gap: '0.5rem', marginTop: '0.75rem' }}
    >
      <input
        type="email"
        name="email"
        required
        placeholder={placeholder}
        style={{
          flex: '1 1 16rem',
          minWidth: 0,
          padding: '0.5rem 0.75rem',
          borderRadius: '0.5rem',
          border: '1px solid rgb(128 128 128 / 0.4)',
          background: 'transparent',
          color: 'inherit',
          fontSize: '0.875rem',
        }}
      />
      <button
        type="submit"
        style={{
          padding: '0.5rem 1rem',
          borderRadius: '0.5rem',
          border: 'none',
          background: '#E6293D',
          color: '#fff',
          fontSize: '0.875rem',
          fontWeight: 600,
          cursor: 'pointer',
        }}
      >
        {button}
      </button>
    </form>
  </div>
)
