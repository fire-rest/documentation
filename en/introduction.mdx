---
title: "Introduction"
description: "Learn how to integrate with Fire, the restaurant management OS."
---

Fire is a restaurant management OS that handles stores, menus, products, and sales channels — including web, app, kiosks, and delivery aggregators (iFood, Keeta, 99, Rappi, and others).

<img className="block dark:hidden rounded-xl mx-auto" style={{maxWidth: '480px'}} src="/images/fire-logo-02.png" alt="Fire" />
<img className="hidden dark:block rounded-xl mx-auto" style={{maxWidth: '480px'}} src="/images/fire-logo-01.png" alt="Fire" />

## Integration model

Fire is the **master system**. It emits events whenever something relevant changes, and it receives orders injected by external systems. Any system that needs to react to those events — or send orders into Fire — integrates through the same interface:

- **Fire emits events** whenever stores, menus, or products change, and whenever an order status is updated. Events are delivered to your system as webhook calls over HTTPS.
- **Fire receives orders** injected by your system via the REST API.

Your system subscribes to the events it cares about and acts on them however it needs to.

<Note>
  A common use case is an aggregator orchestrator — a system that connects Fire to multiple delivery aggregators (iFood, Keeta, 99, Rappi, etc.) and handles publication and order flow between them. The integration works the same way for any other type of client.
</Note>

## Data flows

**Publication flow** — Fire emits events that your system must propagate to downstream systems (aggregators, billing platforms, ERPs, or any system that needs to stay in sync):

<img className="rounded-xl" src="/images/publication-flow.png" alt="Publication flow diagram" />

**Order flow** — your system sends orders into Fire and receives status updates back:

<img className="rounded-xl" src="/images/order-flow.png" alt="Order flow diagram" />

## What you build

Your integration needs to:

| Responsibility | Direction |
|---|---|
| Expose an HTTPS endpoint to receive events | Fire → Your system |
| Validate incoming event signatures | Fire → Your system |
| Process publication events (store, menu, product) | Fire → Your system |
| Inject orders into Fire using your API key | Your system → Fire |
| Handle order status updates from Fire | Fire → Your system |

Both receiving events and calling the API require authentication. See [Authentication](/en/authentication) for how credentials are issued and how to use them on each side.

## Next steps

<CardGroup cols={2}>
  <Card title="Key concepts" icon="book" href="/en/key-concepts">
    Understand the core building blocks of the Fire integration.
  </Card>
  <Card title="Authentication" icon="lock" href="/en/authentication">
    Set up your API credentials and webhook signature validation.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/en/quickstart">
    Get your integration running end to end.
  </Card>
  <Card title="Webhook setup" icon="webhook" href="/en/configuration/webhook-setup">
    Register your endpoint to receive Fire events.
  </Card>
</CardGroup>
