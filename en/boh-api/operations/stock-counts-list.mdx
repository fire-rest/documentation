---
title: "List stock counts"
api: "GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/operations/stock-counts"
description: "Returns stock counts for a vendor with optional filters."
authMethod: "none"
---

<ParamField header="x-api-key" type="string" required>
  API key with `inventory:read` scope.
</ParamField>

<ParamField path="vendorId" type="string" required>
  Vendor UUID.
</ParamField>

<ParamField query="limit" type="number">
  Results per page (1–200, default 50).
</ParamField>

<ParamField query="offset" type="number">
  Pagination offset (default 0).
</ParamField>

<ParamField query="include_cancelled" type="boolean">
  Include cancelled counts. Default `false`.
</ParamField>

<ResponseField name="items" type="object[]">
  Array of stock count objects.
</ResponseField>

<ResponseField name="total" type="number">
  Total matching records.
</ResponseField>

<ResponseExample>
```json 200
{
  "items": [
    {
      "id": "cnt_01j5k...",
      "store_id": "str_main_01...",
      "scope": "PARTIAL",
      "count_type": "DAILY",
      "status": "COMPLETED",
      "counted_at": "2026-08-07T06:00:00.000Z",
      "created_at": "2026-08-07T06:01:00.000Z"
    }
  ],
  "total": 1,
  "limit": 50,
  "offset": 0
}
```
</ResponseExample>
