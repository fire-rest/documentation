---
title: "Identity"
description: "Resolve your account, list vendors, stores, and suppliers. Start here to discover the IDs you need for all other API calls."
---

The identity endpoints are your starting point. They let you confirm which account your API key is attached to, discover your vendors, and enumerate the stores and suppliers you will reference in catalog and operations calls.

**Required scopes:** `catalog:read` for most reads; `catalog:write` for creates and updates.

---

## Me

### Get my account context

<api>GET https://boh.api.fire.rest/api/v1/public/identity/me</api>

Returns the account and API key information derived from the `x-api-key` header. Use this call to verify connectivity and to discover your `account_id` before making writes.

<ParamField header="x-api-key" type="string" required>BOH API key.</ParamField>

<ResponseField name="account_id" type="string">The account UUID associated with this key.</ResponseField>
<ResponseField name="api_key" type="object">
  `{ id, key_prefix }` — the key's UUID and the non-secret prefix for display.
</ResponseField>
<ResponseField name="currency_id" type="string">UUID of the account's configured currency (or `null` if not set).</ResponseField>
<ResponseField name="currency" type="object">
  `{ id, code, name, symbol, decimal_places }` — full currency detail, or `null`.
</ResponseField>

<ResponseExample>
```json 200
{
  "account_id": "acc_01j5k...",
  "api_key": {
    "id": "key_01j5k...",
    "key_prefix": "boh_live_xxxx"
  },
  "currency_id": "cur_01...",
  "currency": {
    "id": "cur_01...",
    "code": "USD",
    "name": "US Dollar",
    "symbol": "$",
    "decimal_places": 2
  }
}
```
</ResponseExample>

---

## Vendors

A **vendor** represents a BOH-configured operating entity (a restaurant brand or outlet). Most other endpoints include a `{vendorId}` path parameter that must be a valid vendor UUID for your account.

### List vendors

<api>GET https://boh.api.fire.rest/api/v1/public/identity/vendors</api>

Returns all vendors for your account. Use this to discover vendor IDs.

<ResponseField name="vendors" type="object[]">
  Array of vendor objects: `id`, `account_id`, `external_vendor_id`, `name`, `status`, `metadata`, `created_at`, `updated_at`.
</ResponseField>

<ResponseExample>
```json 200
{
  "vendors": [
    {
      "id": "vnd_01j5k...",
      "account_id": "acc_01j5k...",
      "external_vendor_id": "brand-main",
      "name": "My Restaurant Brand",
      "status": "ACTIVE",
      "metadata": {},
      "created_at": "2026-01-01T00:00:00.000Z",
      "updated_at": "2026-01-01T00:00:00.000Z"
    }
  ]
}
```
</ResponseExample>

---

### Get a vendor

<api>GET https://boh.api.fire.rest/api/v1/public/identity/vendors/{vendorId}</api>

Returns a single vendor by UUID.

<ParamField path="vendorId" type="string" required>Vendor UUID.</ParamField>

---

## Stores

Stores represent physical locations (kitchens, warehouses, outlets). Operations such as goods receipts, stock counts, and transfers all target a specific store.

### List stores

<api>GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores</api>

<ParamField path="vendorId" type="string" required>Vendor UUID.</ParamField>
<ParamField query="limit" type="number">Results per page (1–200, default 50).</ParamField>
<ParamField query="offset" type="number">Pagination offset (default 0).</ParamField>
<ParamField query="status" type="string">Filter by `ACTIVE` or `INACTIVE`.</ParamField>

<ResponseField name="items" type="object[]">
  Array of store objects: `id`, `account_id`, `code`, `name`, `timezone`, `status`, `external_store_id`, `created_at`, `updated_at`.
</ResponseField>
<ResponseField name="total" type="number">Total matching records.</ResponseField>
<ResponseField name="limit" type="number">Current page size.</ResponseField>
<ResponseField name="offset" type="number">Current offset.</ResponseField>

<ResponseExample>
```json 200
{
  "items": [
    {
      "id": "str_main_01...",
      "account_id": "acc_01j5k...",
      "code": "STR-001",
      "name": "Main Kitchen",
      "timezone": "America/Mexico_City",
      "status": "ACTIVE",
      "external_store_id": "store-main",
      "created_at": "2026-01-01T00:00:00.000Z",
      "updated_at": "2026-01-01T00:00:00.000Z"
    }
  ],
  "total": 1,
  "limit": 50,
  "offset": 0
}
```
</ResponseExample>

---

### Get a store

<api>GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores/{storeId}</api>

Returns a single store with full detail (includes `metadata`).

<ParamField path="vendorId" type="string" required>Vendor UUID.</ParamField>
<ParamField path="storeId" type="string" required>Store UUID.</ParamField>

---

### Create a store

<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores</api>

<ParamField body="name" type="string" required>Display name (max 200 chars).</ParamField>
<ParamField body="code" type="string">Short alphanumeric code.</ParamField>
<ParamField body="timezone" type="string" required>IANA timezone identifier (e.g. `America/Mexico_City`).</ParamField>
<ParamField body="external_store_id" type="string">Your system's store identifier (used in operation payloads instead of UUID).</ParamField>
<ParamField body="metadata" type="object">Free-form extra data.</ParamField>
<ParamField body="external_user_id" type="string">Actor for audit.</ParamField>

---

### Update a store

<api>PATCH https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores/{storeId}</api>

Partial update — only include fields you want to change.

---

### Archive / unarchive a store

<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores/{storeId}/archive</api>
<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/stores/{storeId}/unarchive</api>

---

## Suppliers

Suppliers are the vendors you purchase from. They are referenced in goods receipts and purchase orders.

### List suppliers

<api>GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers</api>

<ParamField path="vendorId" type="string" required>Vendor UUID.</ParamField>
<ParamField query="limit" type="number">Results per page (1–200, default 50).</ParamField>
<ParamField query="offset" type="number">Pagination offset.</ParamField>
<ParamField query="include_archived" type="boolean">Default `false`.</ParamField>

<ResponseField name="items" type="object[]">
  Array of supplier objects: `id`, `account_id`, `external_supplier_id`, `code`, `name`, `email`, `phone`, `tax_id`, `is_active`, `created_at`, `updated_at`.
</ResponseField>

---

### Get a supplier

<api>GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers/{supplierId}</api>

Returns a single supplier with `metadata`.

---

### Create a supplier

<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers</api>

<ParamField body="name" type="string" required>Supplier name.</ParamField>
<ParamField body="code" type="string">Short alphanumeric code.</ParamField>
<ParamField body="external_supplier_id" type="string">Your system's supplier identifier. Must be unique within the account.</ParamField>
<ParamField body="email" type="string">Contact email.</ParamField>
<ParamField body="phone" type="string">Contact phone.</ParamField>
<ParamField body="tax_id" type="string">Tax/fiscal identifier.</ParamField>
<ParamField body="metadata" type="object">Free-form extra data.</ParamField>
<ParamField body="external_user_id" type="string">Actor for audit.</ParamField>

---

### Update a supplier

<api>PATCH https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers/{supplierId}</api>

Partial update — only include fields you want to change.

---

### Archive / unarchive a supplier

<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers/{supplierId}/archive</api>
<api>POST https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers/{supplierId}/unarchive</api>

---

### Sync suppliers (bulk upsert)

<api>PUT https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers</api>

See [Catalog: Bulk sync → Sync suppliers](/en/boh-api/catalog-sync#sync-suppliers) for the full reference.
