---
title: "Consultar una solicitud fiscal"
api: "GET /api/v1/external/fiscal/numbering/{fiscalRequestId}"
description: "Trae el estado actual de una solicitud de numeración por su identificador — incluido el veredicto del ente cuando ya llegó."
---

Devuelve **el mismo contrato** que [`POST /numbering`](/es/api-reference/fiscal-documents),
con el estado actualizado. Lo que cambia entre una llamada y otra es
`documentStatus`: nace en `PENDING` y pasa a `AUTHORIZED` cuando el ente se
pronuncia.

<Info>
  **En la operación diaria no deberías necesitarlo.** Los identificadores ya te
  llegan en `data.fiscalRepresentation` de los eventos de la orden, y el veredicto
  del ente en `lastKnown.fiscal.status`. Este endpoint existe para cuando el camino
  normal no alcanza: perdiste la respuesta síncrona por un corte de red, o querés
  confirmar el estado sin esperar el evento.
</Info>

## Autenticación

| | |
|---|---|
| Header | `x-api-key: pk_live_…` |
| Scope | `fiscal:write` |

Es el **mismo scope que para emitir**, a propósito: quien puede numerar puede
consultar lo que numeró. No hay un scope de solo lectura que dé menos.

El *tenant* sale de la clave, nunca del request. Una solicitud de otra cuenta
devuelve `404`, igual que una que no existe — no se distingue una cosa de la otra
para no filtrar qué identificadores son reales.

## Parámetros de ruta

<ParamField path="fiscalRequestId" type="string" required>
  El `fiscalRequestId` que devolvió la numeración. Es un UUID.
</ParamField>

## Respuesta

Idéntica a la de `POST /numbering` — ver
[la referencia completa del contrato](/es/api-reference/fiscal-documents#respuesta).

<ResponseExample>
```json 200 — el ente ya autorizó
{
  "success": true,
  "data": {
    "fiscalRequestId": "9afef543-56ce-4911-a92e-5908366fa980",
    "orderCode": "EC-K004-42-1786579046934",
    "reused": false,
    "countryCode": "EC",
    "requestStatus": "GENERATED",
    "documentStatus": "AUTHORIZED",
    "retryable": false,
    "environment": "PRODUCTION",
    "document": {
      "documentType": "SALE_INVOICE",
      "documentLabel": "FACTURA",
      "documentNumber": "005-004-000000045",
      "authorizationMode": "ONLINE",
      "authorizationLabel": "EMISION NORMAL",
      "issuedAt": "2026-08-13T13:33:41.438Z",
      "compensates": null
    },
    "printing": { "printable": true, "mode": "FISCAL_DOCUMENT", "reason": null },
    "graphic": { "qr": "1208202601000000000000110050040000000451234567817" },
    "failure": null,
    "countryData": {
      "claveAcceso": "1208202601000000000000110050040000000451234567817",
      "establecimiento": "005",
      "puntoEmision": "004",
      "secuencial": "000000045",
      "ambiente": "PRODUCCION"
    }
  }
}
```

```json 200 — Colombia (CO)
{
  "success": true,
  "data": {
    "fiscalRequestId": "c1a7f0e2-9b34-4d21-8e55-3f60ab12cd90",
    "orderCode": "CO-K039-1786901234",
    "reused": false,
    "countryCode": "CO",
    "requestStatus": "GENERATED",
    "documentStatus": "AUTHORIZED",
    "retryable": false,
    "environment": "PRODUCTION",
    "document": {
      "documentType": "SALE_INVOICE",
      "documentLabel": "FACTURA ELECTRONICA DE VENTA",
      "documentNumber": "SETP990000001",
      "authorizationMode": "ONLINE",
      "authorizationLabel": "VALIDACION PREVIA",
      "issuedAt": "2026-08-16T14:21:03.118Z",
      "compensates": null
    },
    "printing": { "printable": true, "mode": "FISCAL_DOCUMENT", "reason": null },
    "graphic": null,
    "failure": null,
    "countryData": {
      "numeroComprobante": "SETP990000001",
      "cufe": "9c4f1e… (96 caracteres hexadecimales)",
      "prefijo": "SETP",
      "numeroDian": "990000001",
      "qrCode": "https://catalogo-vpfe.dian.gov.co/document/searchqr?documentkey=9c4f1e…",
      "ambiente": "PRODUCCION"
    }
  }
}
```

```json 404 — no existe, o es de otra cuenta
{
  "success": false,
  "error": "NOT_FOUND",
  "message": "FiscalRequest not found"
}
```
</ResponseExample>

## Errores

| Código | Cuándo |
|---|---|
| `401` | La API key falta, no existe o está revocada |
| `403` | La key no tiene el scope `fiscal:write` |
| `404` | El `fiscalRequestId` no existe **o pertenece a otra cuenta** |

## Relacionado

- [Solicitar numeración fiscal](/es/api-reference/fiscal-documents) — el `POST` que la crea
- [Listar solicitudes de una orden](/es/api-reference/fiscal-documents-query) — cuando solo tenés el `orderCode`
- [Datos de impresión fiscal](/es/api-reference/fiscal-print) — lo que lee la caja para reimprimir
