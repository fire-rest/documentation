---
title: "Listar proveedores"
api: "GET https://boh.api.fire.rest/api/v1/public/vendors/{vendorId}/identity/suppliers"
description: "Devuelve todos los proveedores del vendor con filtros opcionales."
authMethod: "none"
---

<ParamField header="x-api-key" type="string" required>
  API key de BOH con scope `catalog:read`.
</ParamField>

<ParamField path="vendorId" type="string" required>
  UUID del vendor.
</ParamField>

<ParamField query="limit" type="number">Resultados por página (1–200, default 50).</ParamField>
<ParamField query="offset" type="number">Offset de paginación (default 0).</ParamField>
<ParamField query="status" type="string">Filtrar por `ACTIVE` o `INACTIVE`.</ParamField>

<ResponseField name="items" type="object[]">
  Array de objetos de proveedor: `id`, `account_id`, `name`, `external_supplier_id`, `status`, `metadata`, `created_at`, `updated_at`.
</ResponseField>
<ResponseField name="total" type="number">Total de registros coincidentes.</ResponseField>
<ResponseField name="limit" type="number">Tamaño de página actual.</ResponseField>
<ResponseField name="offset" type="number">Offset actual.</ResponseField>

<ResponseExample>
```json 200
{
  "items": [
    {
      "id": "sup_01j5k...",
      "account_id": "acc_01j5k...",
      "name": "Proveedor Fresco S.A.",
      "external_supplier_id": "proveedor-fresco",
      "status": "ACTIVE",
      "metadata": {},
      "created_at": "2026-01-01T00:00:00.000Z",
      "updated_at": "2026-01-01T00:00:00.000Z"
    }
  ],
  "total": 1,
  "limit": 50,
  "offset": 0
}
```
</ResponseExample>
